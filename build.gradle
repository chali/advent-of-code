repositories {
    mavenCentral()
}

apply plugin: 'scala'

version = '1.0'

dependencies {
    compile('org.scala-lang:scala-library:2.11.7')
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}